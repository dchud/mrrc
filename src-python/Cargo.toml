[package]
name = "mrrc-python"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
mrrc = { path = ".." }
serde_json = "1.0"

[lib]
name = "_mrrc"
crate-type = ["cdylib"]
